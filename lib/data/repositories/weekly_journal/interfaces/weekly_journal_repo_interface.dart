abstract class IWeeklyJournalRepository {
  // 

  // Future<WeeklyJournalData> createWeeklyJournal(WeeklyJournalData weeklyJournal);
  // Future<List<WeeklyJournalData>> getWeeklyJournals(int writerId);
  // Future<WeeklyJournalData> getWeeklyJournal(int id);
  // Future<int> updateWeeklyJournal(WeeklyJournalData weeklyJournal);
  // Future<int> deleteWeeklyJournal(int id);
  // Future<List<WeeklyJournalData>> queryWeeklyJournals(WeeklyJournalQuery query);
  // Future<int> countWeeklyJournals(int writerId);
  // Future<int> countWeeklyJournalsByDateRange(int writerId, DateTime startDate, DateTime endDate);
  // Future<List<WeeklyJournalData>> getWeeklyJournalsByDateRange(int writerId, DateTime startDate, DateTime endDate);
  // Future<T> transaction<T>(Future<T> Function() action, {bool requireNew = false});
} 